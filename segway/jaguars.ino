
void initJaguars()
{
  jagnet.Initialize();
  
//  jagnet.SendConstants(id,mode,P,I,D);//id is id of jaguar. P I D are the constants to send.
//  //For mode your choices are PMODE,SMODE,IMODE. Position,Speed,Current respectivly.Must be capitalized!
//  
//  jagnet.ConfigPIDsensor(id,mode,sensortype,value);//for mode choices are PMODE or SMODE
//  //for sensor type options are ENCODER INV_ENCODER QUAD_ENCODER POTENTIOMETER 
//  //INV_ENCODER means inverting encoder
//  //for value this is turns of potentiometer, or lines per turn on encoder.
//  
//  jagnet.SetVcompRamp(id,voltage ramp,compensation ramp);//for use with jagnet.SetVcomp()
//  // both rated in mVolts per milisecond
//  //*neither of these values are required, but they help reduce inrush current draw.
//  
//  jagnet.SetVramp(id,voltage ramp);//for use with jagnet.SetPower
//  //rated in mVolts per milisecond
//  //*this isn't required, but it helps to reduce inrush current draw.
//  
//  jagnet.SetMaxVout(id,voltage);//voltage is the high voltage the jag can output
//  jagnet.SetBrakeCoast(id,setting);//for setting, choices are BRAKE COAST JUMPER
//  //*MUST BE CAPITALIZED!
//  
//  //soft limit is an imaginary limit switch in position mode
//  jagnet.EnableSoftLimit(id);//enables softlimit
//  jagnet.DisableSoftLimit(id);//disables softlimit
//  jagnet.ConfigSoftLimitReverse(id,GL,posisition)//for GL use GREATERTHAN or LESSTHAN
//  //MUST BE CAPITALIZED, position is in rotations
//  jagnet.ConfigSoftLimitForward(id,GL,posisition)//for GL use GREATERTHAN or LESSTHAN
//  //MUST BE CAPITALIZED, position is in rotations

}

